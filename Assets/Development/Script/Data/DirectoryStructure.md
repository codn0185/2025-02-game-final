# 프로젝트 파일 구조

아래는 본 게임 프로젝트의 주요 폴더 및 파일 구조입니다.

``` markdown
Application.persistentDataPath/
└── data/
    └── profiles/
        └── {UID}/
            ├── UserData.json
            ├── GameData.json
```
